<nav class="navbar navbar-expand navbar-light bg-warning">
    <div class="collapse navbar-collapse justify-content-center" id="navbarNavDropdown">
      <ul class="navbar-nav ">
        <li class="nav-item dropdown">
          <% if current_user %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.nickname %>
            </a>
            <a class="nav-link" href="#" id="crystals" data-toggle="dropdown">
              <%= current_user.crystal.quantity %> Crystals
            </a>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: 'nav-item btn btn-dark ' %>
            <%= link_to "Sign up", new_user_registration_path, class: 'nav-item btn btn-success ' %>
          <% end %>

            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item" href="#">Action</a>
              <a class="dropdown-item" href="#">Another action</a>
              <a class="dropdown-item" href="#">Something else here</a>
              <%= link_to "Sign out",  destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              
            </div>
        </li>
      </ul>
    </div>
  </nav>
